#include <iostream>

#include "board.hpp"

int main() {
  Board b;
  InitBoard(b);
  
 // DropDiskToBoard(b,DiskType::kPlayer1,4);
  
//InitBoard(b);
//DropDiskToBoard(b,DiskType::kPlayer1,4);
// InitBoard(b);

 //std::cout << (BoardToStr(x)) << std::endl;
 
}